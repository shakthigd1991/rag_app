## RAG Test Project

* Pinecone index is created automatically (`rag-index`) with `cosine` similarity.
* OpenAI model used: **gpt-4o-mini** (can be swapped).
* Document chunks are set to 1500 characters with 200 overlap.

---

## Next Steps

* Add support for multiple document uploads.
* Enhance chat memory with LangChain memory classes.
* Deploy the app on **Streamlit Cloud** or **Docker**.

---
